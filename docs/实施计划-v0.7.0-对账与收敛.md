# GoDDI v0.7.0 实施计划：对账与收敛

> 状态：B7.1、B7.2、B7.3、B7.4、B7.6 已完成；B7.5 已完成新旧真实二进制的可行路径与边界验证，但旧二进制不具备 restore/闸门命令，跨版本拒绝只能由新二进制闸门负责；未提交、未推送、未发布。
> 基线：v0.6.0 发布代码 `9d69d63`；当前实施分支 `workbuddy/impl-v0.6.0`。
> 原则：只把已有代码、测试和演练证据写成可复核结论；单机模拟不冒充真实多主机网络分区。

## 一、对账结论

外部企业级 DDI 方案中列为后续能力的 DHCP HA、IPAM 状态机与联动、配置版本 / diff / 回滚、`/ready`、备份恢复、CI 与 e2e，已在 v0.6.0 完成或有明确边界，不重复实施。

本阶段聚焦真实遗留：

- R-01：配置发布的多节点协调；
- R-02：配置修订保留与清理、首条修订 diff 语义；
- R-03：IPAM 多连接并发与容量证据；
- R-04：真实多进程 DHCP→DNS 事件追平；
- R-05：readiness 日志、耗时、额度阈值证据；
- R-06：使用第二个真实二进制验证恢复闸门；
- R-07：真实掉电、relay、多主机网络分区；
- R-08：DHCP 热路径与“内存 LeaseStore + 异步落库”目标的差异对账；
- R-09：TLS、DNSSEC、容量、告警和文档声明一致性审计。

## 二、批次与依赖

| 批次 | 内容 | 状态 |
|---|---|---|
| B7.1 | 基线复核、遗留台账、边界重写 | 已完成 |
| B7.2 | 配置发布协调、修订保留策略 | 已完成（单节点治理；多节点协调仍为 R-01） |
| B7.3 | IPAM 多连接并发与容量证据 | 已完成（并发唯一性与 WAL/FULL 前提；非容量压测） |
| B7.4 | 三进程数据面追平与 readiness 变化断言 | 已完成 |
| B7.5 | 第二真实二进制恢复与升级证据 | 已完成（新版本同版恢复、旧 schema 前移；旧二进制无 restore/闸门命令，跨版本拒绝边界由新二进制单向验证） |
| B7.6 | 安全、功能声明与文档一致性复核 | 已完成（DNSSEC/Prometheus/TLS/声明边界；现场告警仍待验证） |

依赖关系：B7.2、B7.3、B7.5、B7.6 可在 B7.1 后分批推进；B7.4 已作为第一批闭合并为后续容量 / 运维证据提供进程级测试形状。

## 三、B7.4 实施结果（2026-09-12）

### 3.1 修改文件

- `scripts/w14h_split_outage_drill.py`
  - 在控制库 `BEGIN IMMEDIATE` 写锁窗口产生 DHCP→DNS durable outbox 事件；
  - 断言事件留在 DHCP 本地库、`dhcp_dns_event_dirty` 保留、控制库暂时没有该事件；
  - 断言控制库拒绝写入日志与 readiness `degraded`；
  - 断言同一 readiness 级别不重复刷日志；
  - 释放写锁后断言事件上行、dirty 标记清空、append-only 事件事实行保留；
  - 断言 DNS 副本消费事件并写入 DHCP A 记录，真 UDP 查询返回预期地址；
  - 断言 readiness 恢复 `ok`，普通 DNS A 记录下行仍恢复。

- `docs/实施计划-v0.6.0.md`
  - 将 W14-h / 控制库不可用期间 DDNS 事件追平遗留改为已由 B7.4 进程级演练闭合；
  - 更新断言数量与 append-only 事件语义；
  - 保留单机写锁、非特权 DHCP、非真实多主机分区等边界。

### 3.2 验证证据

最终演练命令：

```bash
PATH="/opt/homebrew/bin:$PATH" python3 scripts/w14h_split_outage_drill.py
```

结果：

- 三进程启动：6/6；
- A 基线：14/14；
- B 控制库不可写窗口：15/15；
- C 恢复与下行消费：18/18；
- 总结：三节全部通过，退出码 0。

附加验证：

- `go test ./...`：通过；
- `go vet ./...`：通过；
- `golangci-lint run ./...`：0 issues；
- Python 脚本语法检查与 `git diff --check`：通过。

### 3.3 必须保留的边界

- 三进程运行在同一台机器，共用同一个 SQLite 控制库；停止 control 进程本身不会使共享数据库不可写；
- `BEGIN IMMEDIATE` 只模拟写不可用，WAL 下读仍可进行，不等同于真实多主机读分区；
- 脚本直接写租约和 DHCP→DNS 事件，绕过真实 `lease.Manager` / `enqueueDNSEvent`，不等同于真实 DHCP REQUEST/ACK；
- 演练覆盖几十秒级追平，不覆盖长时间积压、真实掉电、relay 或升级迁移。

## 四、B7.2 实施结果（2026-09-12）

### 4.1 首条修订 diff 语义

- `Service.DiffRevisions` 允许 `from=0`，按空对象与目标修订比较，返回全部新增字段；`to` 必须为正数。
- API `GET /api/v1/config/revisions/{id}?with_changes=true` 不再把首条修订误报为空变更，和 `PublishResult.Changes` 语义一致。
- 现有 `TestPublish_FirstRevisionIsNumberedOne`、新增首条 diff 测试及 API 回归共同钉住该契约。

### 4.2 修订保留与清理

新增 `Service.PruneRevisions(resource_type, resource_id, keep)`，当前采用显式维护调用而非隐式后台删除：

- `keep >= 1`，仅清理指定资源的旧 `applied` 修订；`staged` / `failed` 证据不自动删除；
- 保留最新 `keep` 条修订；即使超出窗口，最后一条 `applied` 仍保留；
- 被 `config_release_outbox.revision_id` 引用的修订不删除；
- 删除在单个事务内完成并返回删除行数，便于后续接入 CLI、定时维护或审计日志；
- 当前未实现多节点发布协调，B7.2 的已完成范围是单节点配置治理与可审计清理边界，R-01 继续保留。

验证：

- `go test ./internal/configver ./internal/api/handler`：通过；
- 覆盖首条 diff、keep 窗口、最后 applied、outbox 引用保护、非法 keep/type。

## 五、B7.5 实施结果（2026-09-12）

### 5.1 真实二进制与恢复矩阵

- 从历史 `v0.5.2` tag 构建旧二进制 `/tmp/goddi-b75/goddi-old`，当前工作树构建新二进制 `/tmp/goddi-b75/goddi-new`；版本输出分别为 `0.5.2` 与 `0.6.0`。
- 新二进制迁移空源库成功，control schema 到 25；使用相同 JWT / encryption 测试密钥创建加密快照 `/tmp/goddi-b75/drill/new-snapshot.goddi-snap`。
- 新二进制将该新快照恢复到空目标成功，恢复前闸门报告“本二进制可以读取该归档”，control 文件成功替换。
- 旧二进制的命令面只有 `migrate/serve/unlock` 等旧命令，没有 `backup/restore`，也没有迁移 `--check` 或恢复闸门；因此不能把“旧二进制拒绝新归档”伪造为已执行的旧 CLI 证据。旧二进制读取新 schema 文件后，旧迁移器未降级 schema，但它本身没有 fail-closed 的前置闸门，说明 R-06 的关键保护来自 `0.6.0` 新闸门，而不是旧版本兼容性。
- 用旧迁移集合创建 schema 15 的旧库快照（临时 Go harness 仅打包 `control`，明文仅用于无密钥旧 schema 夹具），新二进制恢复到空目标成功，报告 schema 15 落后于本二进制最高 25；随后 `goddi migrate` 应用 016–025，control 达到 schema 25。
- 新二进制升级后的 `migrate --status` 报告 control `25 / 25，无待应用`；dnsdata/leases 因旧夹具未创建而各有 5 条待应用，属于快照中明确 absent 的可选数据面，不是 control 升级失败。

### 5.2 B7.5 结论与边界

B7.5 的真实证据闭合范围是：当前二进制对同版加密快照可恢复；旧 schema 快照可被当前二进制接受并迁移到当前 ceiling；新二进制在恢复写入前执行 migration ceiling 与产品版本关系判断。旧 `v0.5.2` 二进制没有恢复子命令和前置闸门，故不能声称完成“旧二进制主动拒绝新归档”对称测试；该能力需在未来仍保留 restore CLI 的相邻版本上补测。未验证真实掉电、跨主机文件传输损坏、密钥丢失和数据面数据库均已存在的完整升级场景。

## 六、下一批建议

1. R-01：多节点配置发布协调仍需外部部署拓扑和一致性方案，不以单机 SQLite 维护调用冒充已完成。
2. R-07/R-09：真实掉电、relay、多主机网络分区与现场 Prometheus 告警作为外部验收条件推进。

## 七、B7.3 实施结果（2026-09-12)

新增 `internal/ipam/address/address_b73_test.go`，使用同一 WAL 数据库上的两个独立 `*sql.DB` 连接：

- 64 个并发分配请求在两个连接之间交错执行；测试发现并明确处理 SQLite 多写者 `SQLITE_BUSY`，仅对该瞬态冲突重试，不吞掉其他错误；
- 最终断言 64 个地址全部唯一、数据库行数与 `COUNT(DISTINCT ip_address)` 均为 64；
- 读回 `PRAGMA journal_mode=wal` 与 `PRAGMA synchronous=2 (FULL)`，把 WAL/FULL 作为测试前提钉住。

证据边界：这证明当前事务和唯一索引在两个连接下不产生重复分配，并暴露了 SQLite 多写者的锁冲突；它不是 4C/8G 目标机容量压测，也不代表已把生产连接池改为多连接。生产 `internal/database.New` 仍保持 SQLite 单连接，后续若改变连接池必须先复跑该测试并补测锁等待、吞吐和长稳。

验证：`go test ./internal/ipam/address -run 'TestB73_' -count=1` 通过。

## 八、B7.6 实施结果（2026-09-12)

- DNSSEC 对标表已改为实验性表述：当前有密钥/DS/参数入口，但不生成 RRSIG/DNSKEY，不能声称签名链路已完成。
- Prometheus 对标与 README 已明确：`/metrics` 和示例告警规则存在，但真实 Prometheus scrape、`promtool` 校验与告警触发未在本仓闭合。
- 英文 README 的 TLS 说明已与中文部署清单统一：内置 TLS 可用但默认关闭，未启用时必须由 HTTPS 反向代理或可信网络保护。
- 容量与耐久数字继续引用 `docs/耐久与容量-w14.md` 的边界：10C/16GiB 单机、单次运行、分钟级稳态，不能外推 4C/8G、24 小时、真实掉电或真实客户端 ACK。
- 真实掉电、relay、多主机网络分区及现场 Prometheus 告警仍保留为外部验收条件，不伪造为本仓已完成证据。

修改文件：`docs/Technitium对标评估与实施方案.md`、`README.md`、`README.zh-CN.md`。

## 九、当前工作树纪律

本批修改生产测试、配置版本服务、API handler 与计划文档；当前未提交、未推送、未创建新 tag 或 Release。后续批次继续前，先复核工作树状态和本计划的遗留台账。
